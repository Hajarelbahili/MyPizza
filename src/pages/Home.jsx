import { useState } from 'react';
import { pizzas } from '../services/pizzaService';
import { PizzaCard } from '../components/PizzaCard';
export function Home() { const [search, setSearch] = useState(''); const [category, setCategory] = useState('All'); const categories = ['All', ...new Set(pizzas.map(p => p.category))]; const filtered = pizzas.filter(p => (category === 'All' || p.category === category) && `${p.name} ${p.ingredients.join(' ')}`.toLowerCase().includes(search.toLowerCase())); return <><section className="hero"><div><p className="eyebrow">DELIVERED HOT · EVERY DAY</p><h1>Your favorite slice,<br /><i>right on time.</i></h1><p>Hand-stretched dough, premium ingredients, and a little bit of joy in every box.</p><a className="button" href="#menu">Explore menu</a></div><div className="hero-pizza">🍕</div></section><section id="menu" className="menu-section"><div className="section-heading"><div><p className="eyebrow">OUR MENU</p><h2>Made for every craving</h2></div><label className="search"><span>⌕</span><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search pizzas..." /></label></div><div className="filters">{categories.map(item => <button key={item} className={category === item ? 'active' : ''} onClick={() => setCategory(item)}>{item}</button>)}</div>{filtered.length ? <div className="pizza-grid">{filtered.map(pizza => <PizzaCard key={pizza.id} pizza={pizza} />)}</div> : <div className="no-results">No pizzas match that search. Try another taste.</div>}</section></>; }
